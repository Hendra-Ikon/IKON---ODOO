<odoo>
    <data>
        <!-- Create a new XML file (e.g., views/period_popup.xml) -->
        <record id="view_model_period_form" model="ir.ui.view">
            <field name="name">model.period.tree</field>
            <field name="model">model.period</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="period_start"/>
                    <field name="period_end"/>
                    <!-- Add other fields as needed -->
                </tree>
            </field>
        </record>
        

    </data>
</odoo>